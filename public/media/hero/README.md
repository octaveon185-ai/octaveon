# Hero media

Current production-ready fallback:

- `water-resonance-poster.webp` — cinematic photorealistic still used immediately on page load.

Pending final motion assets:

- `water-resonance-loop.webm`
- `water-resonance-loop.mp4`

The component is already configured for muted autoplay, looping, inline playback, a poster fallback and reduced-motion behavior. Until the final loop is supplied, the poster uses only a restrained slow drift.
